# MongoDB connection string - defaults to local MongoDB if not set
DATABASE_URI=mongodb://127.0.0.1:27017/avanti-cms-dev

# Payload secret key - REQUIRED for production, generate with: openssl rand -base64 32
PAYLOAD_SECRET=your-secure-secret-key-here-minimum-32-characters
